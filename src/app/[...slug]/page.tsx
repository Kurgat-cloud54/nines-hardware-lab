import { notFound } from "next/navigation";
import { SiteHeader } from "@/components/marketing/site-header";
const content: Record<string, { title: string; copy: string }> = { about: { title: "A laboratory built for critical hardware.", copy: "NiNes Hardware Lab provides component-level repair and engineering analysis for enterprise and industrial infrastructure." }, services: { title: "Repair services for enterprise infrastructure.", copy: "From switches and routers to power systems and PCB assemblies, each repair follows a disciplined diagnostic and QA process." }, equipment: { title: "Equipment we repair.", copy: "We support networking, telecom and industrial systems from leading enterprise manufacturers." }, industries: { title: "Supporting critical industries.", copy: "ISPs, telecom operators, data centres, government departments and enterprise IT teams depend on resilient equipment." }, contact: { title: "Talk to an engineering specialist.", copy: "Our team will help scope a repair, quotation or service requirement." }, rma: { title: "Request a repair (RMA).", copy: "Create a secure customer account to start a repair request and receive trackable RMA documentation." } };
export default async function MarketingPage({ params }: { params: Promise<{ slug: string[] }> }) { const { slug } = await params; const item = content[slug.join("/")]; if (!item) notFound(); return <><SiteHeader/><main className="grid-bg min-h-[calc(100vh-5rem)]"><section className="mx-auto max-w-5xl px-5 py-28"><p className="text-xs font-bold uppercase tracking-wider text-sky-400">NiNes Hardware Lab</p><h1 className="mt-4 max-w-3xl text-5xl font-semibold leading-tight">{item.title}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-slate-400">{item.copy}</p></section></main></>; }
