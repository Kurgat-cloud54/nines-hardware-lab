"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { createClient } from "@/lib/supabase/client";
export default function LoginPage() { const [message,setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const { error } = await createClient().auth.signInWithPassword({email:String(form.get("email")),password:String(form.get("password"))}); setMessage(error?.message ?? "Signed in. Redirecting…"); if (!error) window.location.assign("/portal"); } return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="w-full max-w-md rounded-lg border border-blue-900 bg-panel p-8"><Link href="/" className="text-sm text-sky-400">← NiNes Hardware Lab</Link><h1 className="mt-7 text-3xl font-semibold">Customer portal</h1><p className="mt-2 text-sm text-slate-400">Secure access to repairs, reports and RMAs.</p><label className="mt-7 block text-sm">Email<input required name="email" type="email" className="mt-2 w-full rounded border bg-ink p-3"/></label><label className="mt-4 block text-sm">Password<input required name="password" type="password" className="mt-2 w-full rounded border bg-ink p-3"/></label><button className="mt-6 w-full rounded bg-electric p-3 text-sm font-bold">Sign in</button>{message && <p role="status" className="mt-4 text-sm text-slate-300">{message}</p>}<p className="mt-6 text-sm text-slate-400">New customer? <Link href="/auth/sign-up" className="text-sky-400">Create an account</Link></p></form></main>; }
