"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { createClient } from "@/lib/supabase/client";
export default function SignUpPage() { const [message,setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const { error } = await createClient().auth.signUp({email:String(form.get("email")),password:String(form.get("password")),options:{emailRedirectTo:`${location.origin}/auth/callback`}}); setMessage(error?.message ?? "Check your email to confirm your account."); } return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="w-full max-w-md rounded-lg border border-blue-900 bg-panel p-8"><Link href="/" className="text-sm text-sky-400">← NiNes Hardware Lab</Link><h1 className="mt-7 text-3xl font-semibold">Create an account</h1><p className="mt-2 text-sm text-slate-400">Start and track a repair request securely.</p><label className="mt-7 block text-sm">Work email<input required name="email" type="email" className="mt-2 w-full rounded border bg-ink p-3"/></label><label className="mt-4 block text-sm">Password<input required minLength={12} name="password" type="password" className="mt-2 w-full rounded border bg-ink p-3"/></label><button className="mt-6 w-full rounded bg-electric p-3 text-sm font-bold">Create secure account</button>{message && <p role="status" className="mt-4 text-sm text-slate-300">{message}</p>}<p className="mt-6 text-sm text-slate-400">Already registered? <Link href="/auth/login" className="text-sky-400">Sign in</Link></p></form></main>; }
