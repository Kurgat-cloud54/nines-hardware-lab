"use client";
import Image from "next/image";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { useState } from "react";
const links = [["Home","/"],["About us","/about"],["Services","/services"],["Equipment","/equipment"],["Industries","/industries"],["Gallery","/gallery"],["RMA portal","/rma"],["Contact","/contact"]] as const;
export function SiteHeader() { const [open,setOpen] = useState(false); return <header className="sticky top-0 z-50 border-b border-blue-950/80 bg-ink/95 backdrop-blur"><nav className="mx-auto flex h-24 max-w-7xl items-center px-5"><Link href="/" className="relative h-20 w-24 shrink-0 overflow-hidden"><Image src="/images/nines-logo.png" alt="NiNes Hardware Lab" fill sizes="96px" className="scale-125 object-contain" priority /></Link><div className="ml-10 hidden items-center gap-6 lg:flex">{links.map(([label,href]) => <Link key={href} href={href} className="text-xs font-bold uppercase tracking-wide text-slate-200 hover:text-blue-400">{label}</Link>)}<Link href="/rma" className="rounded-sm bg-electric px-4 py-3 text-xs font-bold uppercase text-white">Request repair</Link></div><button aria-label="Toggle navigation" className="ml-auto lg:hidden" onClick={() => setOpen(!open)}>{open ? <X/> : <Menu/>}</button></nav>{open && <div className="border-t border-blue-950 bg-ink px-5 pb-5 lg:hidden">{links.map(([label,href]) => <Link onClick={() => setOpen(false)} key={href} href={href} className="block border-b border-blue-950 py-4 text-sm font-semibold">{label}</Link>)}</div>}</header>; }
